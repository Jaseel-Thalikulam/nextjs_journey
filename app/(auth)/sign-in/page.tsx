import Link from 'next/link'
import React from 'react'

const signIn = () => {
  return (
    <div>
       <Link href="/">Go to Home</Link>
      signIn
    </div>
  )
}

export default signIn

// Sign-in and sign-up are grouped in the `auth` folder, which acts as a route group. A folder name enclosed in "()" is called a route group.
